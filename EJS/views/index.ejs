<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <!-- Displaying the vaariable -->

    <!-- <%= name.toUpperCase()%> -->

    <!-- Conditional rendring -->

    <% if(userid === 409){%>
<h1>userId: <%= userid %> </h1> 
        <% } else if(userid===40){ %>
            <h1>UserId:<%= userid %></h1>
            <% } %>

</body>
</html>